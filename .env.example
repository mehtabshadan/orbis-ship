# Environment configuration for Next.js Auto-Updater

# Production environment
NODE_ENV=production

# Enable updater in development (optional)
# ENABLE_UPDATER=true

# GitHub repository configuration (configure in lib/updater/config.ts instead)
# GITHUB_REPO=your-username/your-repo-name

# Update interval in milliseconds (configure in lib/updater/config.ts instead)
# UPDATE_INTERVAL=3600000

# PM2 application name (optional)
# PM2_APP_NAME=orbis-ship